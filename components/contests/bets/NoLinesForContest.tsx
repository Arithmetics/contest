import { Center, Text } from '@chakra-ui/react';

export default function NoLinesForContest(): JSX.Element {
  return (
    <Center marginTop={'30vh'}>
      <Text fontSize="2xl">No lines set for this contest.</Text>
    </Center>
  );
}
