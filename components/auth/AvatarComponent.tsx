// components/terminal-component
import Avatar from 'react-avatar-edit';

export default function TerminalComponent(): JSX.Element {
  return (
    <Avatar
      width={400}
      imageWidth={300}
      height={300}
      //   onCrop={this.onCrop}
      //   onClose={this.onClose}
      //   onBeforeFileLoad={this.onBeforeFileLoad}
      //   src={this.state.src}
    />
  );
}
